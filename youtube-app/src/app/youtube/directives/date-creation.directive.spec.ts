import { DateCreationDirective } from './date-creation.directive';

describe('DateCreationDirective', () => {
  it('should create an instance', () => {
    const directive = new DateCreationDirective();
    expect(directive).toBeTruthy();
  });
});
