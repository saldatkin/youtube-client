import FilterWordPipe from './filter-word.pipe';

describe('FilterWordPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterWordPipe();
    expect(pipe).toBeTruthy();
  });
});
